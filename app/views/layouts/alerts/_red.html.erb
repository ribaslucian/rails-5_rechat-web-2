<% if !flash[:red].nil? %>
  <script type="text/javascript">
    $(document).ready(function () {
        $.toast({
            text: '<%= flash[:red] %>',
            position: 'top-center',
            hideAfter: 5000,
            loaderBg: '#666',
            // icon: 'warning',
            // showHideTransition: 'fade',
            bgColor: '#a94442',
            textColor: 'white',
            // loaderBg: '#c9ba9b'
            // allowToastClose: true
        });
    })
  </script>

  <% flash[:red] = nil %>

<% end %>